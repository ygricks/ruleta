import { IConfig } from './intefaces';

export const RuletaConfig: IConfig = {
    // start drawing board from next point
    start: { x: 50, y: 30 },
    // cell size
    grid: { width: 76, height: 70 },
    // cell border line width
    lineWidth: 2
};
